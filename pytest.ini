[pytest]
minversion = 8.0
testpaths = tests
pythonpath = .
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = 
    -v
    --strict-markers
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: mark test as slow

